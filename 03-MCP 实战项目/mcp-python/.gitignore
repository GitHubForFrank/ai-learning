# ---- Python ----
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# 虚拟环境（uv / venv / virtualenv）
.venv/
venv/
env/
.python-version

# 打包产物
build/
dist/
*.egg-info/
*.egg
wheels/

# 测试 / 类型检查缓存
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.coverage
.coverage.*
htmlcov/

# Jupyter
.ipynb_checkpoints/

# 本地 secrets
.env
.env.local

# ---- mcp-idea-plugin-analyzer 专属 ----
# analyze 运行时产物：<input>-analysis/ 目录常与插件同目录生成，不提交
# 本目录内 samples/ 目录下的产物也不提交，但 samples/ 本身保留
samples/*-analysis/
samples/**/work/

# work/ 是 analyze.py 解压插件的临时目录，见 --work 参数
work/

# ---- 注意：以下文件必须提交，不要加进来 ----
# pyproject.toml / uv.lock / package-lock.json / tools/*.jar
