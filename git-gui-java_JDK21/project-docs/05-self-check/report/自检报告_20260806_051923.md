# 自检报告

> **时间**：2026-08-06 05:19:23
> **范围**：`.trae/` `project-docs/` `app-backend/` `app-db/`
> **门禁**：mvn compile 未执行（本地无 mvn） | mvn test 未执行 | check-refs 已执行（markdown 链接 + BR 覆盖度）

---

## 一、统计概览

| 维度 | 数值 |
| ---- | ---- |
| 检查文件数 | 213（150 .java + 53 .md + 8 .sql + 2 .properties） |
| P0（致命） | 0 |
| P1（重要） | 8 |
| P2（一般） | 37 |
| P3（建议） | 3 |
| 合计 | 48 |

---

## 二、问题列表

### P0 — 致命

无致命问题。

---

### P1 — 重要

| 编号 | 文件:行号 | 检查项 | 问题 | 修复建议 |
| ---- | --------- | ------ | ---- | -------- |
| P1-001 | `MainController.java:1376` | E2 | `@FXML onInit()` 在 JavaFX 线程同步调用 `repositoryService.initRepository()`（内部执行同步 `git init`），可冻结 UI 数秒 | 改为 `AsyncUiLoader.submitWrite` + `Platform.runLater` 切换为等待面板 |
| P1-002 | `SwitchDialog.java:563` | E2 | `doCheckout()` 由 OK 按钮 ActionEvent 触发（FX 线程），内部同步调用 `gitOperationService.checkout()`（阻塞 git checkout），可冻结 UI | 将 checkout 任务提交到 `AsyncTaskService.submitWrite`，用 ProgressDialog 替代直接执行 |
| P1-003 | `MainController.java:1338` | E2 | `openRepository()` → `repositoryService.openRepository()` 在鼠标点击事件回调（FX 线程）中执行三次同步 git CLI 调用（`getCurrentBranch`、`isClean`、`listRemotes`），可能卡几百毫秒 | 使用 `AsyncUiLoader.submitRead` 在后台加载 repo 元数据 |
| P1-005 | `GitProcessBuilder.java:201` | E1 | 裸 `new Thread(...)` 创建 cancel/idle watcher 线程，未使用线程池管理；每次 push/pull/fetch/clone 都创建新线程（虽然 Daemon 且生命周期短，但高频时浪费） | 复用 `TaskManager` 中的线程池或使用 `Executors.newCachedThreadPool`（DaemonThreadFactory） |
| P1-006 | `CommitDialog.java`（全类 1700+ 行） | F1 | **严重违反单一职责原则**：CommitDialog 承担了 UI 构建、文件加载/过滤、提交业务、消息编辑辅助、Diff 查看、分组逻辑等 6 种职责，40+ 个 private 方法，修改任意功能都影响同一类 | 拆分为 `CommitDialog`（壳）+ `CommitFileTableController`（文件表格）+ `CommitMessageEditor`（消息编辑）+ `CommitActionHandler`（提交/推送业务） |
| P1-007 | `GitOperationServiceImpl.java:228,239-240,243,272-274` | D1 | `checkout()` 方法中使用字符串拼接构建 `op` 变量（日志记录用的命令描述），如 `"git checkout " + req.getTag()` 等 4 处 | 改为 `String.format("git checkout %s", req.getTag())` 或使用 `List.of(...)` 提取命令字符串 |
| P1-008 | `CliGitExecutor.java:340,341,574,667,701,724` | D1 | `args.add()` 内部使用字符串拼接构建参数值（如 `"refs/heads/" + branch`、`"origin/" + branchName`），属于轻度 CLI 拼接违规 | 改为直接构建 String 或使用 `String.format`；保持 `args.add()` 的 List 结构不破坏 |
| P1-009 | `ProgressDialog.java:368` | E1 | 心跳线程用裸 `new Thread(...)` 创建，每次 `showAndWaitForTask()` 都新建；线程数随并发操作线性增长 | 将心跳逻辑改为 `javafx.animation.Timeline` 或使用 `ScheduledExecutorService` |

---

### P2 — 一般

#### B2: 代码注释缺失

| 编号 | 文件:行号 | 检查项 | 问题 | 修复建议 |
| ---- | --------- | ------ | ---- | -------- |
| P2-001 | `AmendPushedRule.java:20` | B2 | 构造函数 `public AmendPushedRule(SettingsService)` 缺少 `@param` Javadoc | 补充 `@param settingsService` 注释 |
| P2-002 | `CleanFdxRule.java:20` | B2 | 同上 | 同上 |
| P2-003 | `DeleteProtectedBranchRule.java:20` | B2 | 同上 | 同上 |
| P2-004 | `FilterBranchRule.java:20` | B2 | 同上 | 同上 |
| P2-005 | `ForcePushRule.java:20` | B2 | 同上 | 同上 |
| P2-006 | `LargeFileRule.java:24` | B2 | 同上 | 同上 |
| P2-007 | `NoVerifyRule.java:20` | B2 | 同上 | 同上 |
| P2-008 | `ProtectedBranchRule.java:20` | B2 | 同上 | 同上 |
| P2-009 | `RebasePushedRule.java:20` | B2 | 同上 | 同上 |
| P2-010 | `RedLineToggleRule.java:20` | B2 | 同上 | 同上 |
| P2-011 | `RemoteWhitelistRule.java:25` | B2 | 同上 | 同上 |
| P2-012 | `ResetHardRule.java:20` | B2 | 同上 | 同上 |
| P2-013 | `SensitiveFileRule.java:24` | B2 | 同上 | 同上 |
| P2-014 | `AbstractRedLineRule.java:24` | B2 | 构造函数 `protected AbstractRedLineRule(SettingsService)` 缺少 Javadoc | 补充构造函数的 Javadoc 描述 |

#### A1/MD040: Markdown 代码围栏无语言标签

| 编号 | 文件:行号 | 检查项 | 问题 | 修复建议 |
| ---- | --------- | ------ | ---- | -------- |
| P2-015~023 | `project-docs/07-analysis/report/移除JGit全面改用Git-CLI方案分析报告.md:56,122,137,166,258,319,758,794,847` | A1/MD040 | 代码围栏使用 ` ``` ` 无语言标签（9 处，包括 ASCII 架构图、问题描述、目录树、Phase 计划等） | 全部改为 ` ```plaintext ` |
| P2-024~035 | `project-docs/07-analysis/report/Vue3+Vite+JavaFX-WebView-方案分析报告.md:82,115,189,362,385,512,838,881,913,997,1293,1449` | A1/MD040 | 代码围栏使用 ` ``` ` 无语言标签（12 处，包括目录树、架构图、示例对比、流程图等） | 全部改为 ` ```plaintext ` |

#### C5: 代码质量

| 编号 | 文件:行号 | 检查项 | 问题 | 修复建议 |
| ---- | --------- | ------ | ---- | -------- |
| P2-036 | `TaskHandle.java:105,116,132` | C2/C5 | `completeSuccess`/`completeFailure`/`notifyProgress` 方法中使用 `t.printStackTrace()` 代替 logger（3 处），生产环境中 stack trace 不会进入项目日志 | 改为 `log.warn("handler 异常", t)` |
| P2-037 | `DiffViewerDialog.java:99` | E1 | 裸 `new Thread` 创建 diff 加载线程 | 改为 `AsyncUiLoader.submitRead` 或 `TaskManager` 线程池 |

#### G: 跨目录一致性

| 编号 | 问题 |
| ---- | ---- |
| P2-038 | `app-db/` 目录不存在。用户将 `app-db/` 列入自检范围，但该目录在仓库中不存在。数据实际存储在 `~/.git-gui/db/`（运行时目录），数据库脚本在 `app-backend/src/main/resources/db/migration/`。若 `app-db/` 意图是存放脚本副本或 schema 文档，需创建；否则应从检查范围中移除。 |

---

### P3 — 建议

| 编号 | 文件 | 检查项 | 问题 | 修复建议 |
| ---- | ---- | ------ | ---- | -------- |
| P3-001 | `GitOperationService.java` | F4/ISP | 接口含 8 个方法（commit×2、push×2、pull、fetch、sync、checkout），略超 ISP 推荐阈值（7+ 应拆分），但职责高度内聚（均为 Git 核心写操作） | 可选拆分：`GitCommitService` + `GitSyncService` + `GitCheckoutService` |
| P3-002 | `PullDialog.java:155` | E2 | 构造器中同步调用 `gitExecutor.getCurrentBranch()` 获取标题分支名；`getCurrentBranch` 是快速操作但仍在 FX 线程 | 使用 `AsyncUiLoader` 异步获取后在 `Platform.runLater` 中设置标题 |
| P3-003 | `CommitDialog.java:768` | E2 | 右键菜单回调中同步调用 `statusService.getLog()`（内部 `GitProcessBuilder.executeQuietly()` 同步阻塞），数据量小（1-50 commit）影响有限 | 使用 `AsyncUiLoader.submitRead` 后台加载 commit 列表 |

---

## 三、修复优先级建议

1. **P1 本次迭代必须修复**：共 8 项
   - **E2 FX 线程违规**（P1-001~P1-003）：`onInit()` / `openRepository()` / `SwitchDialog.doCheckout()` 在 FX 线程直接执行 git 命令，导致 UI 冻结 ← **最高优先级**
   - **F1 CommitDialog SRP**（P1-006）：1700+ 行单体类难以维护，是长期债务
   - **D1 CLI 拼接**（P1-007、P1-008）：虽然实际执行走 List args，但日志/参数值拼接应规范化
   - **E1 裸线程**（P1-005、P1-009）：高频场景下线程数可优化

2. **P2 择机修复**：共 37 项
   - **B2 注释缺失**（14 项）：RedLine 规则构造函数补充 Javadoc ← 工作量小，建议本迭代顺手修
   - **MD040 围栏无语言标签**（21 项）：分析报告 markdown 规范化
   - **P2-036 printStackTrace**：改 logger
   - **P2-037 DiffViewerDialog 裸线程**：轻量修复
   - **P2-038 app-db/ 不存在**：澄清或创建

3. **P3 下个迭代**：共 3 项
   - ISP 拆分建议（可选）
   - PullDialog/CommitDialog 轻量异步化

---

## 四、验证结果

- [x] markdown 死链检查通过（`project-docs/` 内全部链接有效）
- [x] SQL 迁移脚本 COMMENT 覆盖 100%（8 个文件全部通过）
- [x] RedLine 规则 BR-26~BR-32 覆盖完整（13 个规则类 + 审计日志机制）
- [x] `.trae/rules/general.md` 路径引用全部有效
- [ ] mvn compile 未执行（本地缺少 Maven 环境，需在 CI/CD 环境补做）
- [ ] mvn test 未执行

---

## 五、一键修复指令

> 将以下内容直接复制给大模型，即可批量修复所有问题。

请按编号依次修复以下自检问题：

- P1-001~P1-003 [MainController.java:1338,1376 / SwitchDialog.java:563] E2: FX 线程直接调用 git 命令（`initRepository`/`openRepository`/`checkout`）→ 改为 `AsyncUiLoader` + `Platform.runLater`
- P1-005 [GitProcessBuilder.java:201] E1: 裸 `new Thread` 创建 watcher → 改用 `Executors.newCachedThreadPool`（DaemonThreadFactory）
- P1-006 [CommitDialog.java 全类 1700+ 行] F1: 严重违反 SRP → 拆分为 4 个类（CommitFileTableController / CommitMessageEditor / CommitActionHandler）
- P1-007 [GitOperationServiceImpl.java:228,239-240,243,272-274] D1: 字符串拼接构建命令描述 → 改用 `String.format`
- P1-008 [CliGitExecutor.java:340,341,574,667,701,724] D1: args 参数值字符串拼接 → 规范化
- P1-009 [ProgressDialog.java:368] E1: 裸 `new Thread` 心跳线程 → 改用 `javafx.animation.Timeline`
- P2-001~P2-014 [core/redline/rule/*.java] B2: 14 个 RedLine 规则构造函数缺少 @param Javadoc → 补充
- P2-015~P2-035 [07-analysis/report/*.md] A1: 21 处代码围栏 ` ``` ` 无语言标签 → 改为 ` ```plaintext `
- P2-036 [TaskHandle.java:105,116,132] C5: `printStackTrace()` → 改为 `log.warn(...)`
- P2-037 [DiffViewerDialog.java:99] E1: 裸 `new Thread` → 改用 `AsyncUiLoader`
- P2-038 澄清 `app-db/` 目录是否应存在，或从自检范围中移除
- P3-001~P3-003 可选优化（ISP 拆分 / PullDialog 异步化 / CommitDialog 日志异步化）

**修复完所有问题后，必须确保应用可以打包成功，没有编译性问题。**
