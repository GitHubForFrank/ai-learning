============================================
你是一名资深的 TortoiseGit 使用者，熟悉各个功能，帮我重新审查我的设计文档

============================================
帮我分析一下项目我想使用下面的组合作为前端设计
给我你的建议，推荐或者不推荐，好处和坏处
Vue3 + Vite 打包静态资源 + JavaFX WebView
跟后端 app-backend 同级，app-frontend 作为前端代码开发
后端打包的时候，自动将前端代码打包到后端的 resources 目录下，前端代码可以直接访问
报告文件是 md格式，放在 07-analysis/report 的下面

报告里，要体现开发过程中如何debug测试的步骤，总不会每次都是打包成 jar来测试吧？帮我分析分析

报告写完之后，你作为一名资深的桌面app开发的架构师，重新审查一下，看看有没有什么可以改进的点，有的话直接修改

============================================

我觉得JGit这个三方依赖存在版本依赖问题，需要升级到最新版本，太麻烦了，我想只保留 Git CLI 的功能，不依赖 JGit 的功能，请帮我分析一下，我该怎么做？
报告文件是 md格式，放在 07-analysis/report 的下面

============================================
git-gui 仓库列表右键菜单中，「打开仓库」「仓库设置」「刷新」「从列表移除」功能选项冗余，操作逻辑重复，该部分右键菜单设计无实际意义，建议优化精简菜单。
给出你的意见
报告文件是 md格式，放在 07-analysis/report 的下面

============================================
右键功能总是显示延迟，你帮我分析一下，下面那个建议更好
给出你的意见，报告文件是 md格式，放在 07-analysis/report 的下面

优化建议
- 菜单结构预缓存：右键菜单选项是固定的，提前构建好菜单对象，右键时仅做显示，不重新生成
- 状态异步加载：如果菜单项需要显示仓库状态（如是否有变更），改为异步回填，先出菜单再刷新状态
- 去掉右键时的 IO 操作：不要在右键触发瞬间去读 .git 目录、查配置等磁盘操作
- 防抖 / 合并触发：避免快速右键多次触发重复计算























============================================
