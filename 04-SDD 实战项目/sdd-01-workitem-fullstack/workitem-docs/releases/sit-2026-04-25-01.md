---
env: sit
date: 2026-04-25
seq: 01
status: planned
spec-tag: v1.0.0
build-sha: <TBD,P2 阶段填镜像 SHA>
epic: EPIC-01-add-login
prepared-by: <TBD>
approved-by: null
executed-at: null
---

## 1. 本次晋级目标

把 Task001(最小登录功能)首次部署到 SIT 环境,作为本项目首个完整跨 4 仓的端到端 Task,验证 SDD 多仓发布流程是否真正跑通。

## 2. 涉及 Task

- Task001 — see [tasks/Task001-add-login.md](../tasks/Task001-add-login.md)

## 3. Flag 变更(diff)

| Flag | 上一态(SIT) | 本次变更后 | 关联 Task |
|---|---|---|---|
| `feat_task001_login` | (不存在,本次新增)| **ON** | Task001 |

## 4. 上游环境验证状态

- 本次为 SIT 首次晋级,无更上游环境;验证依据为合入前的 PR-level 测试矩阵:
  - 全量 TC-U / TC-IT / TC-FE 必绿(`mvn test` + `npm test:unit` 退出码 0)
  - 全量 TC-R-API / TC-R-DB / TC-R-MIG-02/03/04 必绿(回归矩阵)
  - L1 手工 HT-17 ~ HT-22 + HT-FE-09 ~ HT-FE-12 已在开发者本地验证

## 5. DDL / 数据影响

- Flyway 脚本范围:V1 ~ V4(首次部署,V1 task 表 + V2 app_user + V3 login_log + V4 admin 种子)
- V4 含 INSERT 默认管理员;若 SIT 已有数据(本次首次部署应该没有),`INSERT` 会因 `username` 唯一索引冲突失败 —— P2 阶段评估是否改 `INSERT IGNORE` 或前置清表
- 无数据回填 / 索引重建窗口需求

## 6. 回滚预案

- 优先方案:关 `feat_task001_login` flag(秒级);此时:
  - 前端登录页仍可访问但禁用提交(由 flag 控制按钮 disable)
  - 后端 `/api/login` / `/api/me` 返回 5001(若实现 graceful disable)或 404
  - task API 的 JWT 拦截**不可关**(本 Task 引入了 BR-15 全局鉴权);**因此一旦上线就回不到"裸跑"状态**
- 兜底方案:若发现严重缺陷 → revert PR + 重新部署上一 build SHA(回到无 admin / 无 task 表状态);DDL 不回滚(additive)
- **不允许**:DDL 回滚 / 手工 DELETE app_user

## 7. 风险与已知未决

- BCrypt strength=12 在 SIT MySQL 容器上的登录耗时实测(~200ms 单次),纳入观察;若超 P95 SLO(`monitoring §6.1` 默认 300ms)则:
  - 短期:接受 SIT 慢一点,UAT/PROD 评估
  - 中期:开 Task002 调 strength 至 11(走 Expand-Contract:加 `password_hash_v2` 列,双写,逐步切)
- traceparent 在前端旧浏览器(若用户用)生成失败 → 后端 `TraceparentFilter` 兜底生成 + WARN 日志
- 默认密码 `12346#@&` 落地 PROD 是不可接受的 —— UAT 晋级 release plan 必须先轮转此密码

## 8. 执行后回填(executed 状态时填)

- 实际执行时间:<待填>
- 实际生效的 flag 配置:<待填>
- Lessons:<待填>
