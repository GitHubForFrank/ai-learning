<?xml version="1.0" encoding="UTF-8"?>
<!--
    [SDD-TASK: Task001]
    [SDD-SPEC: 03-技术方案.md §9.3 + conventions/monitoring-conventions.md §4.0]
    Logback 外挂 + springProfile 分环境路由
    pattern 含 trace_id / workitem_id MDC 字段(由 TraceparentFilter 注入)
-->
<configuration>

    <springProperty scope="context" name="log.path"        source="logging.file.path"/>
    <springProperty scope="context" name="log.name"        source="logging.file.name"/>
    <springProperty scope="context" name="log.level.root"  source="logging.level.root"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>[%date{yyyy-MM-dd HH:mm:ss.SSS}] [%t] [%-5level] [%X{trace_id:-}] [%X{workitem_id:-}] [%logger{36}] %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>debug</level>
        </filter>
    </appender>
    <appender name="ASYNC_CONSOLE" class="ch.qos.logback.classic.AsyncAppender">
        <discardingThreshold>0</discardingThreshold>
        <queueSize>10000</queueSize>
        <appender-ref ref="CONSOLE"/>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${log.path}/${log.name}.log</file>
        <encoder>
            <pattern>[%date{yyyy-MM-dd HH:mm:ss.SSS}] [%t] [%-5level] [%X{trace_id:-}] [%X{workitem_id:-}] [%logger] %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${log.path}/history/${log.name}-%d{yyyy-MM-dd}-%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>50MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
    </appender>
    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <discardingThreshold>0</discardingThreshold>
        <queueSize>10000</queueSize>
        <appender-ref ref="FILE"/>
    </appender>

    <!-- dev / 默认:Console + File 都开 -->
    <springProfile name="!sit &amp; !uat &amp; !prod">
        <root level="${log.level.root}">
            <appender-ref ref="ASYNC_CONSOLE"/>
            <appender-ref ref="ASYNC_FILE"/>
        </root>
    </springProfile>

    <!-- SIT / UAT / PROD:仅 File -->
    <springProfile name="sit | uat | prod">
        <root level="${log.level.root}">
            <appender-ref ref="ASYNC_FILE"/>
        </root>
    </springProfile>

</configuration>
